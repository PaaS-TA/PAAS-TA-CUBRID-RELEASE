# abort script on any command that exits with a non zero value
set -e

# Percona binary Linux build
tar zxvf cubrid/CUBRID-10.2.1.8849-de852d6-Linux.x86_64.tar.gz
(
    cp -a CUBRID ${BOSH_INSTALL_TARGET}
)

